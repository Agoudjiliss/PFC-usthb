# Endpoints pour service Rasa séparé

# Action server (si utilisé)
action_endpoint:
  url: "http://0.0.0.0:5055/webhook"

# Tracker store (base de données des conversations)
tracker_store:
  type: InMemoryTrackerStore

# Event broker (optionnel)
# event_broker:
#   type: pika
#   url: amqp://localhost

# Model server (pour servir les modèles)
model_server:
  url: "http://0.0.0.0:5005"

# Configuration pour API REST
rest:
  enable_api: true